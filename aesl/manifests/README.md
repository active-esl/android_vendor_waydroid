# Source locks

`lineage-20-lock.xml` is generated by `aesl/scripts/resolve-lock.sh` and reviewed
before commit. It is intentionally absent from the bootstrap commit: inventing a
lock without resolving the complete Android source graph would give false
reproducibility.
